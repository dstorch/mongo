# -*- mode: python -*-

Import("env")

env = env.Clone()

env.SConscript(
    dirs=[
        'i18n',
        'common',
    ],
)

env.Library(
    target='icu',
    LIBDEPS=[
        'icu_common',
        'icu_i18n',
    ],
)
